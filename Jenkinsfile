@Library('path')

pipeline {
  agent any
  stages {
    stage('Build') {
      steps {
        script {
          for (int i=0; i < 3; i++)
          {
              echo 'Hip, hip hurray!'
          }
        }
 
      }
    }
  }
}
